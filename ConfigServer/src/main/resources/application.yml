server:
  port: 8087
  
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Abhik-k/microservices-config-server
          clone-on-start: true
              